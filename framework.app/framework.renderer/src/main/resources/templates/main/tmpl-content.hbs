{{! Content Component }}
<section>
    <header class="major">
        <h2>{{title}}</h2>
    </header>
    {{#rows}}
        {{#single-column}}
            {{> tmpl-content-single }}
        {{/single-column}}
        {{#double-column}}
            {{> tmpl-content-double }}
        {{/double-column}}
        {{#triple-column}}
            {{> tmpl-content-triple }}
        {{/triple-column}}
    {{/rows}}
</section>
